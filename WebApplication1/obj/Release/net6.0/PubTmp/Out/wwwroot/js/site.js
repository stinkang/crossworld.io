import React from 'react';
import ReactDOM from 'react-dom';
import CommentBox from './Tutorial.js';
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
    <React.StrictMode>
        <CommentBox />
    </React.StrictMode>
);